!MockEncoder
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality__in: [mode2]}
          depth_range: [1, 2]
      - !EncodeDriver
        with:
          method: encode
          depth_range: [1, 2]
