!MinRanker
requests:
  on:
    SearchRequest:
      - !LogInfoDriver {}
      - !Chunk2DocRankDriver
        with:
          depth_range: [0, 1]
          traverse_on: [chunks, matches]
      - !LogInfoDriver {}
