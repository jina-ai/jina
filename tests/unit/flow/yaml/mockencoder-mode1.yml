jtype: MockEncoder
requests:
  on:
    IndexRequest:
      - jtype: FilterQL
        with:
          lookups: {modality__in: [mode1]}
          traversal_paths: ['c']
      - jtype: EncodeDriver
        with:
          method: encode
          traversal_paths: ['c']
