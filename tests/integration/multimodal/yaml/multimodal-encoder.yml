!ConcatenateMultiModalEncoder
with:
  position_by_modality:
    modality1: 0
    modality2: 1
metas:
  py_modules: multimodal-concatenate.py
requests:
  on:
    [IndexRequest, SearchRequest]:
      - !MultimodalDriver
        with:
          traversal_paths: ['r']
