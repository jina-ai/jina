!MyEncoder
metas:
  name: myenc  # a customized name
  workspace: /tmp/blah
  py_modules: tests/unit/peapods/remote/yamls/components.py
requests:
  on:
    [IndexRequest, SearchRequest]:
      - !Blob2PngURI {}
      - !EncodeDriver {}
      - !ExcludeQL
        with:
          fields:
            - buffer
            - chunks
