!ConcatenateMultiModalEncoder
with:
  {}
metas:
  py_modules: multimocal-concatenate.py
requests:
  on:
    [IndexRequest, SearchRequest]:
      - !MultimodalDriver
        with:
          traversal_paths: ['r']
