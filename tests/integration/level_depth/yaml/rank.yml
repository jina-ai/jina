!MinRanker
requests:
  on:
    SearchRequest:
      - !LogInfoDriver {}
      - !Chunk2DocRankDriver
        with:
          depth_range: [1, 1]
          traverse_on: matches
      - !LogInfoDriver {}
