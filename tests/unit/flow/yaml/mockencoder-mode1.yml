!MockEncoder
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality__in: [mode1]}
          recur_depth_range: [1, 2]
      - !EncodeDriver
        with:
          method: encode
          recur_depth_range: [1, 2]
