!MockEncoderReduce
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality: mode2}
          recur_depth_range: [1, 2]
      - !EncodeDriver
        with:
          method: encode
          recur_depth_range: [1, 2]
