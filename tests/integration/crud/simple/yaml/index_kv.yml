jtype: BinaryPbIndexer
with:
  index_filename: doc.gzip
metas:
  name: docIndexer
  workspace: $JINA_TOPK_DIR

requests:
  on:
    IndexRequest:
      - jtype: KVIndexDriver
        with:
          executor: docIndexer
          traversal_paths: ['r']
    SearchRequest:
      - jtype: KVSearchDriver
        with:
          executor: docIndexer
          traversal_paths: ['r']
    UpdateRequest:
      - jtype: KVIndexDriver
        with:
          method: update
          executor: docIndexer
          traversal_paths: [ 'r' ]
    DeleteRequest:
      - jtype: DeleteDriver
        with:
          executor: docIndexer