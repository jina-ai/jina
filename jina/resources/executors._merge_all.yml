!BaseExecutor
with: {}
metas:
  name: merge
requests:
  on:
    [SearchRequest]:
      - !ReduceAllDriver {}
      - !SortQL
        with:
          field: 'score.value'
          traverse_on:
            - chunks
            - matches
    ControlRequest:
      - !ControlReqDriver {}