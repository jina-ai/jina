!MockEncoderReduce
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality: mode1}
          depth_range: [1, 2]
      - !EncodeDriver
        with:
          method: encode
          depth_range: [1, 2]
