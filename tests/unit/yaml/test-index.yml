!NumpyIndexer
with:
  index_filename: tmp2
metas:
  name: test2
  workspace: $TEST_WORKDIR
requests:
  on:
    SearchRequest:
      - !VectorSearchDriver
        with:
          method: query
    IndexRequest:
      - !VectorIndexDriver
        with:
          method: add
    ControlRequest:
      - !ControlReqDriver {}