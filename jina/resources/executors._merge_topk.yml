!BaseExecutor
with: {}
metas:
  name: merge
requests:
  on:
    [SearchRequest]:
      - !ReduceDocsDriver {}
      - !SortMatchesQL
        with:
          field: 'score.value'
    ControlRequest:
      - !ControlReqDriver {}