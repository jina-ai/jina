on:
  SearchRequest:
    - !VectorSearchDriver
      with:
        executor: BaseVectorIndexer
    - !ExcludeQL
      with:
        fields:
          - embedding
          - buffer
          - blob
          - text
    - !KVSearchDriver
      with:
        executor: BaseKVIndexer
        recur_depth_range: [0, 0]
        recur_adjacency_range: [0, 1]
  IndexRequest:
    - !VectorIndexDriver
      with:
        executor: BaseVectorIndexer
    - !ExcludeQL
      with:
        fields:
          - embedding
          - buffer
          - blob
          - text
    - !KVIndexDriver
      with:
        executor: BaseKVIndexer
  ControlRequest:
    - !ControlReqDriver {}