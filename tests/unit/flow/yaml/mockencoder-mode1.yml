!MockEncoder
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality__in: [mode1]}
          recurring_range: [1, 2]
      - !EncodeDriver
        with:
          method: encode
          recurring_range: [1, 2]
