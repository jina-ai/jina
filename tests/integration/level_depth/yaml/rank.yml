!MinRanker
requests:
  on:
    SearchRequest:
      - !LogInfoDriver {}
      - !CollectMatches2DocRankDriver
        with:
          traverse_on: matches
          depth_range: [1, 2]
      - !LogInfoDriver {}
