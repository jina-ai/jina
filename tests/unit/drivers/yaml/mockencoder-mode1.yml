!MockEncoderReduce
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality: mode1}
          traverse_on: [chunks]
          recurring_range: [1, 2]
      - !EncodeDriver
        with:
          method: encode
          traverse_on: [chunks]
          recurring_range: [1, 2]
