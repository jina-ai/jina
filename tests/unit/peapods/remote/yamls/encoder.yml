!MyEncoder
metas:
  name: myenc  # a customized name
  workspace: /tmp/blah
  py_modules: yamls/components.py
requests:
  on:
    [IndexRequest, SearchRequest]:
      - !Blob2PngURI {}
      - !EncodeDriver {}
      - !ExcludeQL
        with:
          fields:
            - buffer
            - chunks
