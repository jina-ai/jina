jtype: OneHotTextEncoder
metas:
  name: encoder
requests:
  on:
    IndexRequest:
      - jtype: EncodeDriver
        with:
          traversal_paths: ['c']
    SearchRequest:
      - jtype: EncodeDriver
        with:
          traversal_paths: ['r']
