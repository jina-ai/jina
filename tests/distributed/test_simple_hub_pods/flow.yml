!Flow
version: '1'
with:
  read_only: true
  restful: true
  port_expose: $JINA_GATEWAY_REST_PORT
pods:
  - name: pod1
    uses: docker://hubpod:test
    host: $JINA_POD1_HOST
    port_expose: 8000
  - name: pod2
    uses: docker://hubpod:test
    host: $JINA_POD2_HOST
    port_expose: 8000
