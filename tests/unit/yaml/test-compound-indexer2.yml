!CompoundExecutor
components:
  - !BinaryPbIndexer
    with:
      index_filename: metaproto
    metas:
      name: test_meta
      workspace: $JINA_TEST_WORKSPACE
  - !NumpyIndexer
    with:
      metric: euclidean
      index_filename: npidx
    metas:
      name: test_numpy
      workspace: $JINA_TEST_WORKSPACE
metas:
  name: real-compound
  workspace: $JINA_TEST_WORKSPACE