!DummySegment
requests:
  on:
    ControlRequest:
      - !ControlReqDriver {}
    [SearchRequest, TrainRequest, IndexRequest]:
    - !SegmentDriver
      with:
        method: transform
        random_chunk_id: true