!MockEncoderReduce
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality: mode2}
          recurring_range: [1, 2]
      - !EncodeDriver
        with:
          method: encode
          recurring_range: [1, 2]
