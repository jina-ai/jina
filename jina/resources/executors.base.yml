!BaseExecutor
with: {}
metas:
  name: clear
requests:
  on:
    [SearchRequest, TrainRequest, IndexRequest, DeleteRequest, UpdateRequest]:
      *  # replace by drivers
    ControlRequest:
      - !ControlReqDriver {}