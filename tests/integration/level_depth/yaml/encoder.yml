!OneHotTextEncoder
metas:
  name: encoder
requests:
  on:
    IndexRequest:
      - !EncodeDriver
        with:
          recurring_range: [0, 1]
      - !LogInfoDriver {}
    SearchRequest:
      - !EncodeDriver
        with:
          recurring_range: [1, 1]
      - !LogInfoDriver {}
