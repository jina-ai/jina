!Flow
pods:
  encoder:
    uses: ${CUR_DIR_LEVEL_DEPTH}/yaml/encoder.yml
    read_only: true
  chunk_indexer:
    uses: ${CUR_DIR_LEVEL_DEPTH}/yaml/index-chunk.yml
    read_only: true
  ranker:
    uses: ${CUR_DIR_LEVEL_DEPTH}/yaml/rank.yml
    read_only: true
  doc_indexer:
    uses: ${CUR_DIR_LEVEL_DEPTH}/yaml/index-doc.yml
    read_only: true
