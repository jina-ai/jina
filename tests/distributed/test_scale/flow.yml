jtype: Flow
version: '1'
with:
  port_expose: 9001
  protocol: http
executors:
  - name: scale_executor
    uses: docker://scale-executor
    shards: 1
    replicas: 2
    timeout_ready: '-1'
    volumes: $JINAD_WORKSPACE:/jinad_workspace
