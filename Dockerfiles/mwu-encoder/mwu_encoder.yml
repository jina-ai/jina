!MWUEncoder
with:
  greetings: hello there!
metas:
  name: my-mwu-encoder
  py_modules: mwu_encoder.py
  workspace: ./
requests:
  on:
    [SearchRequest, TrainRequest, IndexRequest]:
      - !EncodeDriver
        with:
          method: encode