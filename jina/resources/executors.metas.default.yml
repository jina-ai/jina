is_trained: false
is_updated: false
batch_size:
workspace:
name:
on_gpu: false
warn_unnamed: false
max_snapshot: 0
py_modules:
root_workspace: ${{root.metas.workspace}} # needed to extract the `dump path` for inner executor of CompoundExecutors
root_name: ${{root.metas.name}} # needed to extract the `dump path` for inner executor of CompoundExecutors
pea_id: ${{root.metas.pea_id}}  # this may result in self-referred
read_only: false
