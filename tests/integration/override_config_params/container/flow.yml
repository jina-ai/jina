jtype: Flow
with:
  port: 12345
executors:
  - name: name
    uses: docker://override-config-test
    uses_with:
      param1: 50
      param2: 30
    uses_metas:
      workspace: 'different_workspace'
