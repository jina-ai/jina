jtype: BaseEncoder
with: {}
metas:
  name: blah
  workspace: $JINA_TEST_EXEC_WITH_DRIVER
requests:
  on:
    [SearchRequest]:
      - jtype: EncodeDriver
        with:
          method: encode
      - jtype: ExcludeQL
        with:
          fields:
            - embedding
