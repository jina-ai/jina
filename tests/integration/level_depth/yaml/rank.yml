!MinRanker
requests:
  on:
    SearchRequest:
      - !LogInfoDriver {}
      - !CollectMatches2DocRankDriver
        with:
          depth_range: [1, 1]
          traverse_on: matches
      - !LogInfoDriver {}
