!ConcatenateMultiModalEncoder
with:
  field_by_modality:
    modality1: embedding
    modality2: embedding
metas:
  py_modules: multimodal-concatenate.py
requests:
  on:
    [IndexRequest, SearchRequest]:
      - !MultimodalDriver
        with:
          traversal_paths: ['r']
