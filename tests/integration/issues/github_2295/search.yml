!SearchDocIndexer
with:
  index_filename: doc.gz
metas:
  name: doc_idx_search  # a customized name
  workspace: $TEST_2295_WORKSPACE
requests:
  on:
    SearchRequest:
      - !SearchDocDriver
        with:
          traversal_paths: ['r']